---
title: K8S 的安装
description: K8S 的安装教程
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

## 服务器准备

服务器列表如下：

|     服务器 IP     |    服务器名称     | 服务器 CPU 核心数 | 服务器内存大小 | 服务器磁盘大小 |
|:--------------:|:------------:|:-----------:|:-------:|:-------:|
| 172.26.206.252 |  k8s-node-1  |     16      |   32    |   1T    |
| 172.26.206.250 | k8s-master-1 |     32      |   64    |   1T    |
| 172.26.206.253 | k8s-master-2 |     32      |   64    |   1T    |
| 172.26.206.251 | k8s-master-3 |     32      |   64    |   1T    |

服务器系统兼容性如下：

|     服务器操作系统      | 是否兼容 |
|:----------------:|:----:|
| Rocky Linux 10.1 |  是   |

### 准备工作

在安装 K8s 之前，还需要对服务器进行如下的配置。

#### 更新系统依赖 [step]

<Tabs groupId="os" items={['Rocky Linux', 'Ubuntu Linux']}>
    <Tab value="Rocky Linux">
        ```bash
        dnf update -y

        dnf upgrade -y
        ```
    </Tab>
    <Tab value="Ubuntu Linux">
        ```bash
        # TODO
        ```
    </Tab>
</Tabs>

#### 安装常用软件包 [step]

<Tabs groupId="os" items={['Rocky Linux', 'Ubuntu Linux']}>
    <Tab value="Rocky Linux">
        ```bash
        dnf install -y gcc gcc-c++ make
        ```
    </Tab>
    <Tab value="Ubuntu Linux">
        ```bash
        # TODO
        ```
    </Tab>
</Tabs>
